version: '2'
services:
  proxy:
    build:
      context: ./
      dockerfile: ./proxy/static.proxy.dockerfile
    ports:
      - "443:443"
    links:
      - "webserver:webserver"
  webserver:
    build:
      context: ./
      dockerfile: ./src/server/server.dockerfile
